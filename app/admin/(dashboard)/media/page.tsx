import React from "react";
import MediaManager from "@/components/admin/MediaManager";

export const metadata = {
  title: "Media & Assets Admin — Masters Leadership Academy",
};

export default function AdminMediaPage() {
  return <MediaManager />;
}
