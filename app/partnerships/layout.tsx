import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Partnerships & Sponsorship",
  description: "Partner or sponsor with Masters Leadership Academy's seminars, symposiums, and conferences.",
};

export default function PartnershipsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
